
synth1:
On a configuré un port taggé sur un switch.
On a configuré un port non taggé, sur un VLAN donné. Des 2 côtés.
On a créé un vSwitch dans un hyperviseur Proxmox.
On a créé une sous-interface dans un hyperviseur Proxmox.
On a configuré le VLAN natif d'un port taggé.

synth2:
On a créé une sous-interface sur un routeur
On a placé une interface dans une VRF.
On a ajouté une route dans une VRF.
On a configuré un NAT statique.
On a ajouté une route spécifique pour ce PC.